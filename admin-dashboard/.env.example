# Copy this file to .env and set it once Person 2's backend is reachable.
# Leave it unset (or delete .env) to run the dashboard fully on sample data.
#
# Example: VITE_API_BASE_URL=http://localhost:4000
VITE_API_BASE_URL=
