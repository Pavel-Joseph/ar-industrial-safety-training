# Use this public API origin when testing the dashboard locally.
# Vite embeds this value in the browser bundle; never put secrets here.
# Leave it unset to run sample-data mode.
VITE_API_BASE_URL=https://ar-industrial-safety-api.onrender.com
